<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neusoft.bsp.bvoinfor.mapper.ManufacturerMapper">

    <!--    <select id="insert" parameterType="User">-->
    <!--        select * from user where t_id = #{tid} and t_password = #{tpassword}-->
    <!--    </select>-->

    <insert id="insert" parameterType="Manufacturer"  useGeneratedKeys="true" keyProperty="man_id">
        insert into man_manufacturer values(null,#{name_en},#{name_cn},#{gmc_report_type},#{gmc_report_url},#{description}
            ,#{created_by},#{creation_date},#{last_update_by},#{last_update_date},#{remark},#{sts_cd})
    </insert>



</mapper>